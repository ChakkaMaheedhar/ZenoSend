# ---- API auth ----
API_TOKEN=dev-token-change-me

# ---- Database ----
DATABASE_URL=postgresql+psycopg2://sguser:sgpass@db:5432/sg_lite

# --- JWT ---
JWT_SECRET=change-me-super-secret
JWT_ALG=HS256
JWT_EXPIRE_MINUTES=480

# ---- Redis / Celery ----
REDIS_URL=redis://redis:6379/0

# ---- SendGrid Web API (PRIMARY path, same behavior as send_once.py) ----
SENDGRID_API_KEY=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
SENDGRID_EU=false

# If a user types a From that is NOT verified, we can still send by
# falling back to a known verified sender and set Reply-To to the user’s address.
VERIFIED_FROM=info@zenotimeflow.com      # or any one verified address you own
FALLBACK_TO_VERIFIED_FROM=true           # set false to block unverified From's

# ---- SMTP (FALLBACK only; not used when SENDGRID_API_KEY is present) ----
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USERNAME=apikey
SMTP_PASSWORD=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
SMTP_USE_TLS=true
SMTP_USE_SSL=false
SMTP_FROM=rama@saibersys.com

# ---- Validation behavior (keep as you have it) ----
ALLOW_SMTP_PROBE=true
PROBE_BLOCKED_PROVIDERS=true
PROBE_MAIL_FROM=info@zenotimeflow.com
ACCEPT_ALL_PROBES=2
VALIDATION_TIMEOUT=8
